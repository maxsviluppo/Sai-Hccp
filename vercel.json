{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/haccp-enterprise-manager/browser",
  "framework": null,
  "functions": {
    "api/*.ts": {
      "runtime": "@vercel/node"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
